---
title: "Post title"
date: "2026-06-07"
excerpt: "One-line summary shown on the index and used as the page description."
tags: ["tag-one", "tag-two"]
draft: true
---

Intro paragraph — set the problem up in a sentence or two.

## Requirements

- Functional requirement one
- Functional requirement two

## A section heading

Markdown works as expected: **bold**, _italics_, `inline code`,
[links](https://rxnova.dev), and lists.

> Use blockquotes for the key trade-off or takeaway.

### A subsection

Fenced code blocks get syntax highlighting:

```ts
function example(id: number): string {
  return id.toString(36)
}
```

GFM tables render too:

| Metric | Estimate |
| ------ | -------- |
| Reads  | 4k/sec   |
| Writes | 40/sec   |

Diagrams use the `<Mermaid>` component (not a ```mermaid fence):

<Mermaid chart={`
flowchart LR
  Client --> LB[Load Balancer]
  LB --> App[App Servers]
  App --> DB[(Database)]
`} />

## Wrap up

Closing thoughts or where to extend from here.
